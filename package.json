{
  "name": "@kikiutils/lz4-wasm",
  "version": "0.1.0",
  "description": "",
  "author": "kiki-kanri",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kiki-kanri/kikiutils-node-lz4-wasm.git"
  },
  "keywords": [
    "compress",
    "decompress",
    "lz4",
    "wasm"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs"
    }
  },
  "types": "./dist/index.d.ts",
  "files": [
    "./dist",
    "./src"
  ],
  "engines": {
    "node": ">=18.12.1"
  },
  "scripts": {
    "build": "pnpm run build:wasm && ts-project-builder ./src/index.ts --clean --sourcemaps",
    "build:wasm": "sh ./build-wasm.sh",
    "bumplog": "changelogen --bump",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "release": "pnpm run lint && pnpm run build && pnpm run test && changelogen --push --release && npm publish",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@kikiutils/changelogen": "^0.7.0",
    "@kikiutils/eslint-config": "^0.7.3",
    "@kikiutils/tsconfigs": "^3.0.2",
    "@rollup/plugin-wasm": "^6.2.2",
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-project-builder": "3.4.0"
  }
}
